import React from 'react';
// import './Header.css';

const Header = () => {
    return (
        <header className="header">
            <h1>Dashboard</h1>
            <button className="text-blue-800 font-semibold hover:underline">Logout</button>
        </header>
    );
};

export default Header;
